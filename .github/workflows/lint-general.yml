---
name: Lint general

# yamllint disable-line rule:truthy
on:
  pull_request: {}
  push:
    branches:
      - main
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  general:
    # yamllint disable-line rule:line-length
    uses: timmo001/workflows/.github/workflows/lint-general.yml@ff1183decb99b9bd7cea4fe7f1ce6c8f5f10f43b # master

  skills:
    # yamllint disable-line rule:line-length
    uses: timmo001/workflows/.github/workflows/lint-agent-skills.yml@ff1183decb99b9bd7cea4fe7f1ce6c8f5f10f43b # master
    with:
      skill-roots: skills
