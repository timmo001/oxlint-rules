---
name: Lint general

# yamllint disable-line rule:truthy
on:
  pull_request: {}
  push:
    branches:
      - main
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  general:
    # yamllint disable-line rule:line-length
    uses: timmo001/workflows/.github/workflows/lint-general.yml@7f8dd7688477dbdd147aa0e9ec381a4c72e63a0e # master

  skills:
    # yamllint disable-line rule:line-length
    uses: timmo001/workflows/.github/workflows/lint-agent-skills.yml@7f8dd7688477dbdd147aa0e9ec381a4c72e63a0e # master
    with:
      skill-roots: skills
