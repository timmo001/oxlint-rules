---
name: Lint general

# yamllint disable-line rule:truthy
on:
  pull_request: {}
  push:
    branches:
      - main
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  general:
    # yamllint disable-line rule:line-length
    uses: timmo001/workflows/.github/workflows/lint-general.yml@791a98574c1c10eb9cc3ef9f774d7cf62d557675 # master

  skills:
    # yamllint disable-line rule:line-length
    uses: timmo001/workflows/.github/workflows/lint-agent-skills.yml@791a98574c1c10eb9cc3ef9f774d7cf62d557675 # master
    with:
      skill-roots: skills
