---
name: Lint general

# yamllint disable-line rule:truthy
on:
  pull_request: {}
  push:
    branches:
      - main
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  general:
    # yamllint disable-line rule:line-length
    uses: timmo001/workflows/.github/workflows/lint-general.yml@9adeeda6f3225e0fc2d1a45cc8afcd9410067187 # master

  skills:
    # yamllint disable-line rule:line-length
    uses: timmo001/workflows/.github/workflows/lint-agent-skills.yml@9adeeda6f3225e0fc2d1a45cc8afcd9410067187 # master
    with:
      skill-roots: skills
