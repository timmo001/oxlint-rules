---
name: Publish

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published

permissions:
  contents: read
  id-token: write

jobs:
  npm:
    # yamllint disable-line rule:line-length
    uses: timmo001/workflows/.github/workflows/publish-npm-package.yml@7f8dd7688477dbdd147aa0e9ec381a4c72e63a0e # master
    permissions:
      contents: read
      id-token: write

  jsr:
    # yamllint disable-line rule:line-length
    uses: timmo001/workflows/.github/workflows/publish-jsr-package.yml@7f8dd7688477dbdd147aa0e9ec381a4c72e63a0e # master
    permissions:
      contents: read
      id-token: write
