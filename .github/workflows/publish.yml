---
name: Publish

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published

permissions:
  contents: read
  id-token: write

jobs:
  npm:
    # yamllint disable-line rule:line-length
    uses: timmo001/workflows/.github/workflows/publish-npm-package.yml@7ca73488f2c4d16f40e6fbc8afad83ffab7b96fd # master
    permissions:
      contents: read
      id-token: write

  jsr:
    # yamllint disable-line rule:line-length
    uses: timmo001/workflows/.github/workflows/publish-jsr-package.yml@7ca73488f2c4d16f40e6fbc8afad83ffab7b96fd # master
    permissions:
      contents: read
      id-token: write
